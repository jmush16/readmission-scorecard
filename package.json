{
  "name": "post-discharge-gap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Post-Discharge Gap — a free, non-PHI CMS tool: a hospital's readmission/penalty exposure plus where its patients go next (SNF + home health) and how often they bounce back.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:data": "tsx scripts/build-snapshot.ts",
    "benchmark": "tsx cli/benchmark.ts"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  }
}
